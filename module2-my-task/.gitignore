.env
__pycache__/
*.pyc
.venv/
out/
.ruff_cache/

# логи прогонів — це фікстури проєкту, не сміття.
# глобальний ~/.gitignore_global ігнорує "logs", тому вимикаємо це тут явно:
# спершу тека, потім файли — інакше негація не спрацює.
!data/logs/
!data/logs/*.log
