anthropic>=0.40.0
python-dotenv>=1.0.0

# векторний retriever + Ragas-порівняння (достатньо ANTHROPIC-ключа):
sentence-transformers>=3.0
qdrant-client>=1.12        # векторна БД: вбудований :memory: або сервер
ragas @ git+https://github.com/sanyaden/ragas.git@fix/optional-vertexai-imports-langchain-community-0.4
datasets>=2.19
langchain-anthropic>=1.5
langchain-huggingface>=1.2

# фреймворки:
langchain>=1.3
langgraph>=1.2

# MCP-сервер (SDK 1.x = FastMCP, 2.0 = MCPServer — код підтримує обидва):
mcp[cli]>=1.2

# експорт трейсів (опційно, без ключів деградує до локального):
langfuse>=2.50
